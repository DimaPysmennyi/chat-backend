model UsersOnChats {
  user   User      @relation(fields: [userId], references: [id])
  userId Int
  chat   ChatGroup @relation(fields: [chatId], references: [id])
  chatId Int

  @@id([userId, chatId])
}
